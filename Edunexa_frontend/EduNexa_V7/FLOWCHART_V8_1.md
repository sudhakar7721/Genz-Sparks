# EduNexa V8.1 Login Flow

EduNexa Login
├── Student
├── Faculty  (single card)
│   ├── Faculty Login → Faculty Dashboard + existing Faculty features
│   └── HOD Login → HOD Dashboard + existing HOD features
└── Management

The Faculty card is the user-facing name; HOD remains an internal Staff Login Type so both roles share one login section.
